/* global describe, it, cy */

describe('spec path rendering for example-spec', () => {
  // it('should render path content for all paths', () => {
  //   cy.visit('/?spec=example-specs/petstore-example.3.0.json')
  //
  //   cy.get('at-api-spec-path')
  //     .should('have.length', 2)
  // })
  //
  // it('rendering the first path', () => {
  //   cy.visit('/?spec=example-specs/petstore-example.3.0.json')
  //
  //   cy.get('at-api-spec-path').then(paths => {
  //     cy.wrap(paths[0]).within(_ => {
  //       cy.get('header h2').contains('Pet Collection')
  //     })
  //   })
  // })
  //
  // it('rendering the second path', () => {
  //   cy.visit('/?spec=example-specs/petstore-example.3.0.json')
  //
  //   cy.get('at-api-spec-path').then(paths => {
  //     cy.wrap(paths[1]).within(_ => {
  //       cy.get('header h2').contains('ps:pet')
  //     })
  //   })
  // })
})
